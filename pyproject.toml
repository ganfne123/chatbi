[project]
name = "new-chatbi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ganfen", email = "473243033@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.13.0",
    "pymysql>=1.2.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
new-chatbi = "new_chatbi:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
